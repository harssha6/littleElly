---

const {img, title , className,style,index} = Astro.props
import Text from "../system/Text.astro";

---

<div data-appear
  class={`flex-shrink-0 snap-end overflow-visible ${className}`}
>
  <slot />
</div>